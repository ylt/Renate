package co.d3s.ylt.renate.player.protocol;

public class Packet040EntityMetadata extends PlayerPacket {
	// NOTE: this packet once again contains metadata, if you know how to do it,
	// go ahead(ylt?)
}
